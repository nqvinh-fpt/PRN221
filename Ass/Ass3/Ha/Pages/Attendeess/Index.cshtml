@page
@model Ha.Pages.Attendeess.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].RegistrationTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendee[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Attendee) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegistrationTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.EventId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AttendeeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AttendeeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AttendeeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
